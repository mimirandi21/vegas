<%= form_with(model: trans_like) do |form| %>
  <% if trans_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trans_like.errors.count, "error") %> prohibited this trans_like from being saved:</h2>

      <ul>
        <% trans_like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :transportation_id %>
    <%= form.text_field :transportation_id %>
  </div>

  <div class="field">
    <%= form.label :like_id %>
    <%= form.text_field :like_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
