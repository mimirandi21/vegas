<%= form_with(model: dest_category) do |form| %>
  <% if dest_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dest_category.errors.count, "error") %> prohibited this dest_category from being saved:</h2>

      <ul>
        <% dest_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :destination_id %>
    <%= form.text_field :destination_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
