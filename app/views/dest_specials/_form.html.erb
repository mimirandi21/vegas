<%= form_with(model: dest_special) do |form| %>
  <% if dest_special.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dest_special.errors.count, "error") %> prohibited this dest_special from being saved:</h2>

      <ul>
        <% dest_special.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :destination_id %>
    <%= form.text_field :destination_id %>
  </div>

  <div class="field">
    <%= form.label :special_id %>
    <%= form.text_field :special_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
