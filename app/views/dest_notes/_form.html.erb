<%= form_with(model: dest_note) do |form| %>
  <% if dest_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dest_note.errors.count, "error") %> prohibited this dest_note from being saved:</h2>

      <ul>
        <% dest_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :destination_id %>
    <%= form.text_field :destination_id %>
  </div>

  <div class="field">
    <%= form.label :note_id %>
    <%= form.text_field :note_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
