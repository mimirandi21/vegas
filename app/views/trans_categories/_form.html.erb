<%= form_with(model: trans_category) do |form| %>
  <% if trans_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trans_category.errors.count, "error") %> prohibited this trans_category from being saved:</h2>

      <ul>
        <% trans_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :transportation_id %>
    <%= form.text_field :transportation_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
